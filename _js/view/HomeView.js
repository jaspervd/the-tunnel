'use strict';

define([
  'jquery',
  'underscore',
  'backbone',
  '../collection/groups',
  '../classes/settings'
], ($, _, Backbone, Groups, Settings) => {
  var HomeView = Backbone.View.extend({
    initialize: function () {

    },

    render: function () {

    }
  });

  return HomeView;
});
